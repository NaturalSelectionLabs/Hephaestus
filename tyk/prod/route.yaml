apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: tyk-gw
  namespace: tyk
spec:
  entryPoints:
  - web
  - websecure
  routes:
  - kind: Rule
    match: Host(`tyk-gw.rss3.io`)
    services:
    - name: gateway-svc-tyk-headless
      port: 8080
