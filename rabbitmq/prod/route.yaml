apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: rabbitmq
  namespace: guardian
spec:
  entryPoints:
  - web
  - websecure
  routes:
  - kind: Rule
    match: Host(`mq.prod.nsl.xyz`) && PathPrefix(`/`)
    services:
    - name: rabbitmq
      port: 15672
