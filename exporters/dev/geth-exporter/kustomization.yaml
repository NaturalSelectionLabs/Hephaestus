apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


helmCharts:
  - name: geth-exporter
    releaseName: geth-exporter
    repo: https://naturalselectionlabs.github.io/helm-charts
    version: ">=0.1.1"
    valuesFile: values.yaml